#!/bin/sh

if [ $# -eq 1 ]; then
    idx=$1
elif [ $# -eq 0 ]; then
    idx=1
else
    exit 1
fi

git diff -- `git status -s -b | grep -v "^#" | awk '{print$1="";print}' | grep -v "^$" | awk "NR==${idx}"`

#vim:ft=sh
